services:
  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: gradient-cloudflared
    restart: unless-stopped
    network_mode: host
    command: tunnel --no-autoupdate run --token ${TUNNEL_TOKEN}
